version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - yum install -y maven
  build:
    commands:
      - mvn clean package -DskipTests

artifacts:
  files:
    - '**/*.jar'
    - appspec.yml
    - scripts/**
  discard-paths: no